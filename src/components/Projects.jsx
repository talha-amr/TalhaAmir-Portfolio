import React from 'react';
import { ProjectShowcase } from '@/components/ui/project-showcase';

const Projects = () => {
    return (
        <section id="projects" className="bg-[#0b0b0d]">
            <ProjectShowcase />
        </section>
    );
};

export default Projects;
